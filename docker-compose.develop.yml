version: '3.8'
services:
    front:
        image: registry.addamant-work.ru/lms-addamant/addamant-lms/develop
        networks:
            - swarm
            - lms-dev-microservices
        deploy:
          replicas: 1
          placement:
            constraints:
             - node.hostname == develop
networks:
    swarm:
        external: true
    lms-dev-microservices:
        external: true
