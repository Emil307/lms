import { createStyles } from "@mantine/core";

export default createStyles(() => ({
    root: {
        alignItems: "center",
        justifyContent: "space-between",
        padding: 16,
        gap: 16,
    },
}));
