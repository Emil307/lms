import { createStyles } from "@mantine/core";

export const useSidebarItemWithChildrenStyles = createStyles(() => ({
    item: {
        border: "none",
    },
    content: {
        padding: 0,
    },
}));
