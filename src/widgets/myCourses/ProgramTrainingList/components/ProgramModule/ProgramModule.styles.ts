import { createStyles } from "@mantine/core";

export default createStyles((_theme) => ({
    moduleBlockInfo: {
        flexDirection: "column",
        width: "100%",
        maxWidth: 424,
    },
    lessonsBlockInfo: {
        flex: 1,
        flexDirection: "column",
        gap: 8,
    },
}));
