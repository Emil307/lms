import { createStyles } from "@mantine/core";

export default createStyles((theme) => ({
    headingDescription: {
        fontWeight: 500,
        fontSize: 16,
        lineHeight: "24px",
        color: theme.colors.dark[0],
    },
}));
