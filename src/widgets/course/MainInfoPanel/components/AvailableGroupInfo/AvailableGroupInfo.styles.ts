import { createStyles } from "@mantine/core";

export default createStyles(() => ({
    availableGroupInfoContainer: {
        flexWrap: "wrap",
        columnGap: 8,
        rowGap: 8,
        width: "fit-content",
    },
}));
