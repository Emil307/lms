import { createStyles } from "@mantine/core";

export default createStyles(() => ({
    footer: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        paddingTop: 40,
        paddingBottom: 16,
    },
}));
