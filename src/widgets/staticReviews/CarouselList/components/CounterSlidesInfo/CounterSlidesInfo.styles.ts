import { createStyles } from "@mantine/core";

export default createStyles((_theme) => ({
    root: {
        position: "absolute",
        alignItems: "center",
        height: 56,
        width: "fit-content",
        left: 120,
        bottom: 48,
    },
}));
