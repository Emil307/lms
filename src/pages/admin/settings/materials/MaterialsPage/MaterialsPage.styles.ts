import { createStyles } from "@mantine/core";

export default createStyles((theme) => ({
    descriptionModal: {
        fontWeight: 500,
        fontSize: 16,
        lineHeight: "24px",
        color: theme.colors.gray45[0],
    },
}));
