import { createStyles } from "@mantine/core";

export default createStyles(() => ({
    iconContainer: {
        minWidth: 96,
        minHeight: 96,
    },
    description: {
        fontWeight: 500,
        fontSize: "12px",
        lineHeight: "14px",
    },
}));
