import { createStyles } from "@mantine/core";

export default createStyles(() => ({
    iconContainer: {
        minWidth: 96,
        minHeight: 96,
    },
    title: {
        fontWeight: 600,
        fontSize: "20px",
        lineHeight: "24px",
    },
    description: {
        fontWeight: 500,
        fontSize: "12px",
        lineHeight: "14px",
    },
}));
