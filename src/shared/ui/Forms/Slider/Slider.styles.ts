import { createStyles } from "@mantine/core";

export default createStyles((_theme) => ({
    thumb: {
        position: "relative",
    },

    iconChevronRight: {
        position: "absolute",
        bottom: 8,
        padding: 4,
        borderRadius: 8,
    },
}));
