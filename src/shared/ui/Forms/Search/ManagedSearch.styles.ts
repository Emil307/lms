import { createStyles } from "@mantine/core";

export default createStyles((theme) => ({
    nothingFound: {
        paddingBlock: 20,
        fontWeight: 500,
        fontSize: 12,
        lineHeight: "16px",
        color: theme.colors.gray45[0],
        textAlign: "center",
    },
}));
