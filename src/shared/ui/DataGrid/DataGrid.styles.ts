import { createStyles } from "@mantine/core";

export default createStyles(() => ({
    tableWrapper: {
        flexDirection: "column",
        marginTop: 24,
        minHeight: 0,
        flexGrow: 1,
    },
}));
