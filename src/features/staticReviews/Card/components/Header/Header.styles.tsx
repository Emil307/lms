import { createStyles } from "@mantine/core";

export default createStyles((_theme) => ({
    root: {
        flexDirection: "column",
        alignSelf: "flex-end",
        maxWidth: 768,
        marginBottom: 104,
        gap: 48,
    },
}));
