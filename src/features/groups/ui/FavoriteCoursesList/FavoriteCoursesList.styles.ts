import { createStyles } from "@mantine/core";

export default createStyles((theme) => ({
    root: {},
    description: {
        fontWeight: 500,
        fontSize: 12,
        lineHeight: "16px",
        color: theme.colors.dark[0],
    },
}));
