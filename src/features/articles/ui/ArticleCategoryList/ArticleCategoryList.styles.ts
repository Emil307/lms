import { createStyles } from "@mantine/core";

export default createStyles((_theme) => ({
    root: {
        flexDirection: "column",
        gap: 32,
    },
    buttonLoadMore: {
        width: "min-content",
        alignSelf: "center",
    },
}));
